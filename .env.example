# 复制本文件为 .env，再填入你自己的 key：
#   copy .env.example .env        (Windows)
#   cp .env.example .env          (macOS / Linux)
#
# .env 已被 .gitignore 排除，不要提交、不要分享。
# 不填 key 也能跑：服务自动走 SIM 模式（本地模拟，零调用）。

# TypeSafe API key。有它 → LIVE（真实调用）；留空或删掉本行 → SIM。
TYPESAFE_API_KEY=apikey_your_key_here

# 可选。监听端口，默认 8787；被占用时自动顺延到 8788、8789……
# PORT=8787

# 可选。监听地址，默认 127.0.0.1（只本机可访问）。
# 不要改成 0.0.0.0 —— /api/decide 会真实花钱，不能暴露给局域网。
# HOST=127.0.0.1
