# jev-demo 代码审查与修复记录

- **审查对象**：`C:\Users\songdaili-jk\WorkBuddy\2026-09-21-10-57-20\jev-demo`
- **日期**：2026-09-21
- **范围**：`server.js` / `public/app.js` / `public/index.html` / `public/style.css` / `README.md` / `data/*` / `.env` / `start.cmd`
- **验证方式**：`node --check` 语法校验 → 真实启动服务跑接口 → 无头 Chrome 真机渲染截图

## 一、总体结论

项目质量高于一般 demo：零依赖、模式区分（LIVE/SIM/回放）清晰、"绝不混算 + 诚实标注"这条主线做得很扎实，真实调用链路已验证可用。

本次**修复 10 处**（含 3 处安全问题、4 处正确性/一致性问题、3 处健壮性），并同步更新 README。另有 **1 项需要你决策的建议**（见第四节）。

---

## 二、已修复清单

| # | 严重度 | 位置 | 问题 | 修复 |
|---|---|---|---|---|
| 1 | **高** | `server.js` listen | 绑定 `0.0.0.0`，**同网段任何人都能访问**；而 `/api/decide`、`/api/flood-tick{live}`、`/api/warmup` 会真实花钱 | 新增 `HOST=127.0.0.1`（可用环境变量覆盖），`listen` 带 host；端口重试同步修改 |
| 2 | **高** | `server.js` warmup | `/api/warmup` 的 `count` 只被 `EVENTS.length`(1000) 限制，一次可触发 1000 次真实调用（≈$0.026） | 新增 `WARMUP_MAX=200` 硬上限，响应回传 `requested / cappedAt` |
| 3 | 中 | `server.js` 路由 | `/api/status`、`/api/pool` 未校验 method，POST 也能拿到数据 | 补 `req.method === "GET"` |
| 4 | 中 | `server.js` `readBody` | 请求体超 1MB 时 `req.destroy()`，但 `end` 不触发 → **Promise 永久悬挂**，请求泄露 | 补 `close` / `error` 监听 + 幂等 `finish()` |
| 5 | 中 | `server.js` `simDecide` | 概率分布用递减 `left` 分摊，**余数无人认领 / 可能为负**，模拟出的概率和 ≠ 1 | 改为权重归一 + 四舍五入后补回主选项，严格和为 1 |
| 6 | 中 | `app.js` `applyResult` | `fromPool` 一律标「回放」，导致**用 SIM 模式预热出的存档被错标成"真实回放"**，与"诚实标注"原则冲突 | 改以 `result.mode` 判定：`live+池=回放`、`live+即时=真实`、其余=模拟 |
| 7 | 中 | `index.html` + `app.js` | 泳道标题把 `0.85 / 0.6` 写死在 HTML 里，**README 说"改 GATE 即可"但画面不会变** | 给三个标题加 id，boot 时按 `/api/status`.gate 动态渲染（现含 noul 阈值） |
| 8 | 低 | `app.js` `exportCsv` | CSV 公式注入过滤只挡 `=` `+` `@`，**漏了 `-` 与制表/回车** | 正则改为 `/^[=+\-@\t\r]/` |
| 9 | 低 | `app.js` / `server.js` | 模型返回的 `queue.choice`、`legend`、概率键经 `innerHTML` 直插，未转义 | 全部套 `esc()` |
| 10 | 低 | `app.js` 两处 | `state.lats` 运行时无上限；「随机一条」不检查 `slRun.disabled`，可并发触发付费调用 | `lats` 超 300 截断；`runRandom` 加同样的守卫 |

`server.js` 顺带删除 `floodTick` 里未被前端使用的死字段 `idx`。

---

## 三、文档同步（README）

- 「访问地址」补：服务只监听本机、以及为什么（接口会花钱，不能暴露给局域网）
- 「三种运行模式」补：`--warmup` 是命令行专用；HTTP 接口单次上限 200；**用 SIM 模式预热会污染回放池**（会被如实标成"模拟"）
- 接口表：`warmup` 标注单次上限 200
- 「常见改动」：改 `GATE` 一行补注"泳道标题会自动跟着变，无需改 HTML"

---

## 四、需要你决策的建议（未改动）

**1. `.env` 里的 TYPESAFE_API_KEY 建议轮换。**
这是明文凭证明文落盘。README 自己也写了"这个 key 在聊天里出现过，建议用完轮换"。key 已经出现在多个会话/工作区目录里，建议去 TypeSafe 控制台重新生成一把，旧的下线。若要彻底不留痕，可改为只从环境变量注入、磁盘上不放 `.env`。

---

## 五、遗留观察（低优先级，本次未改）

| 观察 | 说明 |
|---|---|
| `?static=1` + `scene=slowmo` 会自动真实调用一次 | 录制定帧的设计如此，但意味着"打开一个链接就花钱"，对外发链接时注意 |
| 写接口无鉴权 | 已通过绑定 `127.0.0.1` 缓解；如未来要放到共享机器，建议再加一层 token |
| 首页「门禁阈值」只显示 `0.85 / 0.6` | 不含 noul 阈值，信息略不全（泳道标题已完整） |
| `--open` 依赖 Windows `start` 命令 | 非 Windows 会打印降级提示，不影响主流程 |

---

## 六、验证证据

| 验证项 | 方法 | 结果 |
|---|---|---|
| 语法 | `node --check server.js` / `public/app.js` | 均 OK |
| 绑定地址 | `netstat` 查 LISTENING | `TCP 127.0.0.1:8797 LISTENING`（此前为 `0.0.0.0`） |
| 方法守卫 | `POST /api/status` / `GET /api/status` | `404` / `200` |
| 概率归一 | SIM 模式 `flood-tick n=12` 逐条求和 | 12 条全部 = 1，异常 0 条 |
| warmup 上限 | SIM 模式 `POST /api/warmup {count:1000}` | `{"warmed":200,"requested":1000,"cappedAt":200}` |
| 真实链路 | LIVE 模式 `POST /api/decide` | `refund conf 0.95 / 761ms / 616 in 97 out` |
| 错误分支 | 空 text `POST /api/decide` | `400` |
| 真机渲染 | 无头 Chrome 截 `?scene=flood` | 泳道标题动态生效、概率条满宽、无 JS 报错浮层 |

截图见 `shots/review/`：`standby.png`、`flood.png`。

> 说明：修复过程中为回归测试临时移开 `.env` 跑 SIM 模式，`/api/warmup` 曾把回放池覆盖成 200 条模拟数据；已用一次真实预热重新生成 60 条 live 存档还原（`data/replay.json`，60 条，mode 全为 live）。

---

## 七、第二轮：数据口径澄清（回应「流水 35 条 / 真实调用 5 次，是不是有假的」）

**结论：数字都是对的，不是假数据。** 差异来自设计——洪流幕默认读回放池。

### 实证（同一 Chrome profile 连跑三幕，服务端日志可交叉核对）

| 步骤 | 画面结果 | 服务端日志 |
|---|---|---|
| 只跑洪流幕 `?scene=flood` | 已决策 **19** 条，右上角徽标 **真实调用 0 次** | 无任何 `[jev]` 行 |
| 再跑一次实测台 `?scene=slowmo&static=1` | — | **1 行** `[jev] 18:24:54 1034ms 603 in / 97 out -> technical (conf 0.96)` |
| 打开数据板 `?scene=board` | 真实调用 **1** 次 · 累计成本 **$0.000025**（正好一次的钱）· 决策流水 **19** 条，其中第 1 行标「真实」、其余 18 行标「回放」 | — |

即：**流水条数 = 真实 + 回放 + 模拟**；洪流幕跑得越久，回放条数越多，真实调用数不变。回放池本身也是真实调用的存档（60 条 live，含 model `jev-1.13.0` 与 usage 回执），只是"存下来重放"。

### 为什么仍会让人误判 → 已修复

| 问题 | 修复 |
|---|---|
| **页脚「Jev 累计成本」含回放/模拟**（上图 $0.000486 / 19 条），而数据板「累计成本」只算真实花费（$0.000025）—— 两个数差 19 倍，看着像有猫腻 | 页脚列改名为「**Jev 折算成本**」并加 tooltip 说明"按已决策条数折算，含回放/模拟，非真实支出"；数据板列改为「**累计成本（真实调用）**」 |
| 数据板只显示「决策流水 N 条」，不体现构成 | 流水标题新增构成明细：`决策流水 19 条 （真实 1 · 回放 18）`，由 `state.history` 按 kind 统计 |
| 「真实调用次数」无口径说明 | 补 tooltip："只统计本页面真实发出的 POST /v1/systemone；回放池与模拟都不计入" |

README 第 6 节补了对应说明（两处成本口径不同、流水条数 ≠ 真实调用次数），避免下次再被同一个问题绊住。

---

## 八、第三轮：概念澄清 + 移除厂商基准模块 + UI 优化

### 1. 「真实 / 回放」到底差在哪（结论先行）

两者**都是真实模型的真实输出**，区别只在"这次展示有没有花一次调用"：

| 标签 | 数据从哪来 | 这次展示花调用/钱吗 |
|---|---|---|
| **真实** | 本页面当场发出的 `POST /v1/systemone`，拿到响应立即上屏 | **花**（约 $0.000025/次） |
| **回放** | 读 `data/replay.json`——当初真实调用后存下来的响应，原样重放 | **不花**（0 调用） |
| 模拟 | 无 key 时的本地模拟概率 | 不花 |

### 2. 「速度」按钮是什么

它调的是**洪流幕的展示节奏**（每条决策的展示间隔 620ms，1×/2×/4×），**与模型响应速度无关**——卡片上显示的 `xxx ms` 始终是真实端到端延迟。真实数据源模式下该按钮本来就会提示"不可调速"（节奏跟着真实延迟走）。为消除歧义，按钮已改名「**洪流节奏**」并加 tooltip。

### 3. 「厂商公开基准参考」模块已整体移除

- 删 `index.html` 整块卡片；数据板改为**单栏全宽**，顶部统计一行四列
- 删 `app.js` 里 `fillBoard` 对四根对比条的引用（原样留着会直接抛 `null.style` 异常、整个数据板白屏）和 `bdRefToggle` 事件
- 删 `style.css` 全部 ref 专用规则（`bd-warn/bd-row/bd-track/bd-fill/bd-num/bd-refhead/bd-refrows/refhidden/bd-grid`），花括号平衡校验通过
- `server.js`：`/api/status` 去掉无人消费的 `bench` 字段；`BENCH` 里四个 demo 数值成死代码，收敛为 `PRICE_PER_MTOK = 0.042`
- README 四处相关描述同步（数据板职责、分镜脚本、第 6 节数据口径）

**残留的厂商口径数字（未删，可选项）**：底部计分板「同量 LLM 估算」和实测台备注里的 `同任务 LLM 估算 $0.01388 / 8.566s` 仍按官方 demo 口径折算。这是唯一还引用厂商数字的地方；要去掉的话删 `app.js` 的 `LLM_COST/LLM_LAT` 与 `index.html` 的 `sbLlm` 格子即可，README 已写明操作。

### 4. UI / 展示优化（本轮）

| 位置 | 改动 |
|---|---|
| 数据板 | 单栏全宽；顶部统计改一行四列；卡片头提示改为"以下全部是本机真实发生的事" |
| 决策流水 | 每行行首加**来源色条**（真实=绿 / 回放=蓝 / 模拟=灰），配合行尾标签，来源一眼可辨 |
| 控制条 | 「速度 1×」→「**洪流节奏 1×**」+ tooltip |
| 待机页 | 描述补预期管理：实测台=当场真实调用，洪流幕=默认回放预录存档（0 调用） |
| 数据板流水标题 | 加 tooltip 解释三种来源及"为什么条数会多于调用次数" |

### 验证

- `node --check server.js / public/app.js`：通过
- id 交叉校验：`app.js` 引用的 id 在 `index.html` 中无一缺失
- 死引用扫描：已删模块的 10 个 class 在 CSS/HTML/JS 中零残留；花括号 218/218 平衡
- **真机截图本轮未完成**：无头 Chrome 在沙箱内开始被强制回收（连第一张都未落盘），UI 改动为静态验证。刷新页面即可看到效果；若布局异常，优先怀疑 `.bd-wrap`/`.bd-live` 两处 grid 改动

---

## 九、第四轮：对外展示版视觉升级（"哇塞"感）

定位：保持浅色主题与紫色主色不变、**所有诚实标注功能不动**，只做质感与动效增强。面向短视频录制/对外宣讲。

### 1. 开场页（对外第一眼）

| 改动 | 效果 |
|---|---|
| 标题 / 主标语改**渐变文字**（深灰→紫→蓝） | DECISION FLOOD 与"所有 AI 都在写字，只有它在做决定"更抢眼 |
| 背景加两团**缓慢漂浮的渐变光晕**（紫/蓝，blur 70px） | 静态画面也有呼吸感 |
| 内容按序**依次升起**（sbRise，0.04–0.64s 级联） | 开场仪式感 |
| 「开始演示」按钮加发光阴影 + **周期高光扫过** | 吸引点击 |
| 统计卡 hover 上浮 | 微交互 |

### 2. 洪流幕（动感核心）

| 改动 | 效果 |
|---|---|
| 事件卡片入场加**微倾斜回弹** | 不再干巴巴地平移 |
| 三问卡片**级联点亮**（0.04/0.09/0.14s 延迟） | 像逐个点亮的指示灯 |
| 每条新决策重触发核心卡入场动画 | 决策有"落位"感 |
| 门禁判定改**盖章弹跳**（gatePunch ×2） | 判定时刻有仪式感 |
| 泳道 chip 加**泳道色左竖条** + 回弹入场 | 分流去向一眼可辨 |
| 计分板 / 泳道计数改**数字滚动（count-up）** | 洪流跑起来数字狂跳，最有"哇塞"感 |
| 顶栏真实调用徽标同步弹跳 | 真实调用发生时全场可见 |

### 3. 实测台（过程感）

| 改动 | 效果 |
|---|---|
| 三问结果卡**数据成型揭示**（blur→清晰 + 上浮，级联） | 像数据在眼前"凝结" |
| 门禁判定卡**盖章动画**（结果返回后重触发） | 落定瞬间最有冲击 |

### 4. 数据板 & 全局

- 流水行级联入场；slogan「不发文字，只做决定。」渐变文字；控制条加投影；幕切换淡入上浮过渡。

### 5. 截图/定帧兼容

新增 `.static` 类（`?static=1` 时挂到 body）：所有动画以 0.001s 瞬时跑完给终态。**不用 `animation:none`**——`.q` 等元素初始 `opacity:0`，禁动画会让它们永远不可见。

### 6. 本轮截图揪出的两个新 bug（已修）

| bug | 根因 | 修复 |
|---|---|---|
| 计数显示成 `-1.23e+41` 天文负数 | count-up 用 `performance.now()`，无头虚拟时钟回跳 → `p` 为负 → `(1-p)^3` 指数爆炸 | `p` 钳制 `[0,1]` |
| 修复后计数又冻结在 0 | count-up 用 `requestAnimationFrame`，**无头/虚拟时钟下 rAF 可能不触发** | 改用 `setTimeout` 步进（虚拟时间可快进、真实浏览器照常） |

### 验证

真机无头截图四幕全过：`shots/review/w_standby.png`、`w_flood.png`（已决策 55 = 21+22+12，三问条/色条正常）、`w_board.png`（单栏全宽、流水构成明细、厂商模块已无）、`w_slowmo.png`（真实调用 1282ms、判定盖章）。CSS 花括号 265/265 平衡，17 个 keyframes 引用完整。

> 环境备注：从 Git Bash 启动 Chrome，`--user-data-dir` 必须给**原生 Windows 路径**（`C:\...`），不能用 MSYS 的 `/c/...`，否则 Chrome 报「无法创建数据目录」并卡死。

---

## 六、上线（2026-09-22）

| 项 | 结果 |
|---|---|
| GitHub | https://github.com/dailyoozoo/jev-demo （public，main） |
| Cloudflare Pages | https://jev-demo.pages.dev |
| 线上模式 | `live`。实测一次判定：`jev-1.13.0`、technical、conf 1、253ms、约 $0.000025 |
| 密钥 | `TYPESAFE_API_KEY` 只在 Pages 生产环境变量里，不在仓库 |
| 公网差异 | 不提供 `/api/warmup`；真实调用每 IP 每分钟 20 次 |

线上后端是 `functions/api/*.js`（Pages Functions），本地仍是 `node server.js`，两边逻辑同源。


